extends layout

block content
  h2 Messages v13 + live chat 🔴
  form#myform(action="/", method="post", autocomplete="off")
    if check == false
      input#username(type="text", name="username", placeholder="Username")
      br
      input#input(type="text", name="message", placeholder="Message")
      input(type="submit", value="Send")
    else 
      input#input(type="text", name="message", placeholder="Message")
      input(type="submit", value="Send")
  
  div#new-messages
  if messages
    dl
      each message in messages
        //- display time set next to the username
        dt= message.username + " - " + message.time
        dd= message.message
  else
    p No messages
  